// ADead-BIB con sintaxis Python-style
// Igual que Python pero compila a binario nativo

fn main() {
    let counter = 0
    
    while counter < 1000000000 {
        counter += 1
    }
    
    println(counter)
}
