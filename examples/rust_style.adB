// ADead-BIB - Ejemplo con sintaxis estilo Rust
// Este archivo demuestra la nueva sintaxis compatible con Rust

// Función principal estilo Rust
fn main() {
    print("ADead-BIB con sintaxis Rust!")
    
    // Variables con let
    let x = 42
    let y = 10
    let result = x + y
    
    print("Resultado calculado")
}

// Función con parámetros tipados
fn add(a: i32, b: i32) -> i32 {
    return a + b
}
