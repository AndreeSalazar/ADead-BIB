#include <cstdio>

int main() {
    int total = 0;
    int pass = 0;
    total++;
    pass++;
    printf("total=%d pass=%d\n", total, pass);
    total++;
    total++;
    printf("total=%d\n", total);
    return 0;
}
