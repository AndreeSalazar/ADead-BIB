// ============================================
// ADead-BIB - Ejemplo 02: Variables y Tipos
// Sintaxis Rust + Python combinada
// ============================================

// --- ESTILO RUST ---
fn rust_style() {
    // Variables con let
    let x = 42
    let y = 10
    let nombre = "ADead-BIB"
    
    // Variables mutables
    let mut contador = 0
    contador = contador + 1
    
    // Constantes
    const PI = 3
    const MAX_VALUE = 1000
    
    print("Variables Rust-style definidas")
}

// --- ESTILO PYTHON ---
def python_style():
    # Asignación directa
    x = 42
    y = 10
    nombre = "ADead-BIB"
    
    # Múltiples asignaciones
    a = 1
    b = 2
    c = 3
    
    print("Variables Python-style definidas")

// --- TIPOS SOPORTADOS ---
fn tipos() {
    // Enteros
    let entero = 42
    let negativo = -17
    let grande = 1000000
    
    // Strings
    let texto = "Hola mundo"
    let otro = "ADead-BIB"
    
    // Booleanos
    let verdadero = true
    let falso = false
    
    print("Tipos: int, string, bool")
}

// Script principal
print("=== Variables en ADead-BIB ===")
