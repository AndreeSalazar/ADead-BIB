#include <stdio.h>

int main() {
    int a = 1, b = 2, c = 3;
    printf("a=%d b=%d c=%d\n", a, b, c);
    
    int x, y;
    x = 10;
    y = 20;
    printf("x=%d y=%d\n", x, y);
    
    int *p, q;
    q = 42;
    printf("q=%d\n", q);
    return 0;
}
