// ADead-BIB v0.5.0 - Vectores
// Operaciones con vectores 2D

fn main() {
    println("=== Vectores ===")
    
    let vx = 3
    let vy = 4
    
    // Magnitud^2 = x^2 + y^2
    let mag = vx * vx + vy * vy
    print("Magnitud^2: ")
    println(mag)
}
