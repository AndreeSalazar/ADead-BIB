// ADead-BIB v0.5.0 - Objetos
// Simulacion de objetos con variables

fn main() {
    println("=== Objetos ===")
    
    // Rectangulo
    let ancho = 5
    let alto = 3
    
    let area = ancho * alto
    print("Area: ")
    println(area)
    
    let perimetro = ancho + ancho + alto + alto
    print("Perimetro: ")
    println(perimetro)
}
